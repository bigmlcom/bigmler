language: python
python:
  - "2.7"
  - "3.6"
# command to install dependencies
install: "python setup.py install"
# command to run tests
script: - export BIGML_USERNAME
- export BIGML_API_KEY
- nosetests
